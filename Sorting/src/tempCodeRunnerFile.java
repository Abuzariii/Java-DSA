nt i = 0; i < nums.length; i++) {
        // if (nums[i] > 0 && nums[i] <= nums.length && nums[nums[i] - 1] != nums[i]) {
        // int correctIndex = nums[i] - 1;
        // int temp = nums[i];
        // nums[i] = nums[correctIndex];
        // nums[correctIndex] = temp;
        // }
        // }

        // System.out.println(Arrays.toString(nums));